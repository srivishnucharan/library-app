{
  "name": "library-app",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "api:dev": "node scripts/api-dev.js",
    "dev:api": "node scripts/api-dev.js",
    "api:start": "node scripts/api-dev.js",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.12",
    "typescript": "^5.5.4"
  }
}
